name: Demo Pipeline

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Run Python File
        run: python main.py

      - name: Force Failure
        run: exit 1
